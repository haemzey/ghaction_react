apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-service
  namespace: restaurant-app
  labels:
    app: customer-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-service
  template:
    metadata:
      labels:
        app: customer-service
    spec:
      containers:
        - name: customer-service
          image: haemzay/customer-service:7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3104
          envFrom:
            - configMapRef:
                name: shared-env
          readinessProbe:
            tcpSocket:
              port: 3104
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 3104
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: restaurant-app
spec:
  selector:
    app: customer-service
  ports:
    - port: 3104
      targetPort: 3104
  type: ClusterIP
