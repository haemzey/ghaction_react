apiVersion: apps/v1
kind: Deployment
metadata:
  name: kitchen-service
  namespace: restaurant-app
  labels:
    app: kitchen-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kitchen-service
  template:
    metadata:
      labels:
        app: kitchen-service
    spec:
      containers:
        - name: kitchen-service
          image: haemzay/kitchen-service:v7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3105
          envFrom:
            - configMapRef:
                name: shared-env
          readinessProbe:
            tcpSocket:
              port: 3105
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 3105
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
  namespace: restaurant-app
spec:
  selector:
    app: kitchen-service
  ports:
    - port: 3105
      targetPort: 3105
  type: ClusterIP
