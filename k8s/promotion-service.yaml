apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotion-service
  namespace: restaurant-app
  labels:
    app: promotion-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: promotion-service
  template:
    metadata:
      labels:
        app: promotion-service
    spec:
      containers:
        - name: promotion-service
          image: haemzay/promotion-service:v7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3110
          envFrom:
            - configMapRef:
                name: shared-env
          readinessProbe:
            tcpSocket:
              port: 3110
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 3110
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: promotion-service
  namespace: restaurant-app
spec:
  selector:
    app: promotion-service
  ports:
    - port: 3110
      targetPort: 3110
  type: ClusterIP
