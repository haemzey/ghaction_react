apiVersion: apps/v1
kind: Deployment
metadata:
  name: menu-service
  namespace: restaurant-app
  labels:
    app: menu-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: menu-service
  template:
    metadata:
      labels:
        app: menu-service
    spec:
      containers:
        - name: menu-service
          image: haemzay/menu-service:v7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3101
          envFrom:
            - configMapRef:
                name: shared-env
          readinessProbe:
            tcpSocket:
              port: 3101
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 3101
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: menu-service
  namespace: restaurant-app
spec:
  selector:
    app: menu-service
  ports:
    - port: 3101
      targetPort: 3101
  type: ClusterIP
