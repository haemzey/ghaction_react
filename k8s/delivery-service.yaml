apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: restaurant-app
  labels:
    app: delivery-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: haemzay/delivery-service:7
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3106
          envFrom:
            - configMapRef:
                name: shared-env
          readinessProbe:
            tcpSocket:
              port: 3106
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 3106
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: restaurant-app
spec:
  selector:
    app: delivery-service
  ports:
    - port: 3106
      targetPort: 3106
  type: ClusterIP
